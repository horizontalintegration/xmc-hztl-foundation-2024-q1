import { Canvas, Meta } from '@storybook/blocks';
import * as ModalWrapperStories from './ModalWrapper.stories';

<Meta of={ModalWrapperStories} />

# Modal Wrapper

## Overview

This guide will show you how to use the Modal-Wrapper component and its available props and features.

## Usage

Use PlainTextWrapper when you want to render Sitecore Single-Line Text, Multi-Line Text, or numeric field data.

## Live Demo

<Canvas of={ModalWrapperStories.WithRemoteTrigger} />

